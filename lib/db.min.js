import mongoose from"mongoose";const URI=process.env.DATABASE.replace("<PASSWORD>",process.env.DATABASE_PASSWORD),connectDB=async()=>{try{if(mongoose.connection.readyState>=1)return void console.log("MongoDB is already connected");await mongoose.connect(URI)}catch(error){throw console.error(`Error connecting to MongoDB: ${error.message}`),new Error(`Error connecting to MongoDB: ${error.message}`)}};export default connectDB;