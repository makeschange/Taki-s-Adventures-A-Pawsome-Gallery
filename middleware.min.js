import{NextResponse}from"next/server";import protectRoute from"./middleware/protectRoute";import checkRoutePath from"./middleware/checkRoutePath";export const config={matcher:"/api/:path*"};export default function middleware(req){const url=req.nextUrl.clone(),notFoundRoute=checkRoutePath(req);return notFoundRoute||("/api/signin"!==url.pathname&&"/api/authenticate"!==url.pathname?protectRoute(req):NextResponse.next())}